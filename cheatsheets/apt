--- Install apps ---

sudo apt update			# Updates the package listing
sudo apt install [app/file.deb] # Installs specified application
sudo apt upgrade		# Install latest version of software

--- Removing apps ---

sudo apt remove --purge [app] 	# Remove specified app and configuration files
sudo apt autoremove		# Remove unused packages

--- Information ---

apt list --installed		# List installed packages
apt-cache show [pkg]		# List extra info for app
apt-cache search [pkg]		# Get info on installed package
